# 复合表格示例汇总：结构定义 × 渲染效果 × 填充逻辑

---

## 示例 1：设备详情卡片（kv_grid + tabular + kv_grid）

**对应文件**：[`example_1_device_detail.yaml`](./example_1_device_detail.yaml)  
**底层网格**：8 列

### 数据填充逻辑

```
区段一（基本信息）
  → 执行 SQL，返回 1 行，fields[].col 按列名取值
  → 6 对 KV，cols_per_row=2，每行排 2 对
  → 最终 3 行数据行

区段二（关键指标）
  → 执行 SQL，返回 N 行，每行 → 表格一行
  → columns[].field 按列名取值填入各格

区段三（综合结论）
  → 执行 SQL，返回 1 行，fields[].col 按列名取值
  → 2 对 KV，cols_per_row=1，每行全宽
```

### 渲染效果

```
+═══════════════════════════════════════════ 基本信息 ══════════════════════════════════════════+
│   设备名称   │        三级循环水泵 P-204A        │   设备型号   │            XW-500            │
│   安装位置   │           循环水泵站-A            │   投入时间   │          2022-05-18          │
│   当前状态   │             设备异常              │  7天告警次数 │              8次             │
+═══════════════════════════════════════════ 关键指标 ══════════════════════════════════════════+
│     指标名     │     当前值     │  判断  │                    说明                          │
│   振动幅度     │  7.8 mm/s    │  异常  │  振动异常，建议转入计划性检修                      │
│     温度       │   92.3 °C    │  异常  │  温度超阈值，关注散热状况                          │
│     在线率     │    99.2 %    │  正常  │  在线率良好                                        │
+═══════════════════════════════════════════ 综合结论 ══════════════════════════════════════════+
│   综合判断   │                              设备异常                                        │
│   建议动作   │  停机检修；更换磨损零件；转子平衡检测；过滤器清洗维护；记录异常时间           │
```

---

## 示例 2：多设备横向对比矩阵（tabular 动态列）

**对应文件**：[`example_2_comparison.yaml`](./example_2_comparison.yaml)  
**底层网格**：逻辑列数 = 1 + 设备数（由 SQL PIVOT 列决定）

### 数据填充逻辑

```
区段一（汇总元信息）
  → 参数直填 {time_range}、{device_count}

区段二（对比主表）
  → SQL PIVOT 查询：列名 = 设备ID，行 = 指标
  → headers 中 ~dynamic~ 列自动展开为实际设备 ID 列
  → columns 中 ~dynamic~ 列按 SQL 结果列名自动映射
  → N 台设备 = N 列，模板不写死
```

### 渲染效果（假设 3 台设备）

```
+═══════════════════════════════════════════════════════════════════════+
│   统计周期   │                       近30天                          │
│   纳管设备数 │                        3台                            │
+══════════════════════ 关键指标横向对比 ══════════════════════════════+
│     指标     │      A001      │      A002      │      A003      │
│   在线率(%)  │     99.2       │     97.8       │     98.5       │
│  振动幅度    │  7.8 mm/s 🔴  │  3.2 mm/s 🟢  │  4.1 mm/s 🟢  │
│    温度      │  92.3 °C  🔴  │  78.1 °C  🟢  │  81.2 °C  🟢  │
│  健康评分    │   ★★☆☆☆       │   ★★★★☆       │   ★★★★☆       │
```

---

## 示例 3：动态属性面板（kv_grid key_col/value_col）

**对应文件**：[`example_3_dynamic_kv.yaml`](./example_3_dynamic_kv.yaml)  
**底层网格**：8 列

### 数据填充逻辑

```
区段一（设备标识）
  → 参数直填：fields[].value 含 {$device}、{time_range}

区段二（设备档案）
  → SQL 返回多行：每行 (attr_name, attr_value)
  → key_col="attr_name" → KV 的 key
  → value_col="attr_value" → KV 的 value
  → 行数由数据库决定，模板不预设字段数
  → 按 cols_per_row=2 自动排入多行

区段三（近期运行记录）
  → SQL 返回多行，按 columns[].field 映射列
```

### 渲染效果

```
+═══════════════════════════════════════════ 设备标识 ══════════════════════════════════════════+
│   设备编号   │        KD-PUMP-204A               │   报告周期   │           近30天            │
+═══════════════════════════════════════════ 设备档案 ══════════════════════════════════════════+
│   设备名称   │     三级循环水泵 P-204A             │    型号      │           XW-500            │
│   制造商     │         凯文机械                   │   额定功率   │           55 kW             │
│   安装位置   │         循环水泵站-A               │   投运时间   │         2022-05-18          │
│  维保责任人  │           张工                     │  下次保养    │         2026-06-01          │
↑ 以上行数完全由数据库 device_attributes 表决定，新增属性自动出现在报告中
+════════════════════════════════════════ 近期运行记录 ═════════════════════════════════════════+
│     日期     │  运行时长(h)  │  告警次数  │                  备注                  │
│  2026-03-07  │    23.5      │     3      │              振动偏高                  │
│  2026-03-06  │    24.0      │     1      │                                        │
│  2026-03-05  │    24.0      │     0      │                                        │
```

---

## 三种填充方式对比

| **方式** | **配置字段** | **适用场景** |
|---|---|---|
| **参数直填** | `fields[].value: "{param}"` | 字段固定、值来自用户输入/全局参数 |
| **SQL 单行，字段固定** | `fields[].col` + `source.query` | 字段固定、值来自数据库（一条记录） |
| **SQL 多行，动态 KV** | `source.key_col` + `source.value_col` | 字段名也来自数据库，数量不确定 |
| **SQL 多行，tabular** | `columns[].field` + `source.query` | 明细列表，行数不确定 |
| **SQL PIVOT，动态列** | `~dynamic~` 标记 + PIVOT 查询 | 列数（如设备数）运行时才确定 |
| **AI 属性/总结** | `source.kind: ai_synthesis` | 需要引用多源上下文或知识库进行专家级总结 |

---

## 示例 4：带 AI 专家建议的评估表（AI 总结型）

**对应文件**：[`example_4_ai_summary.yaml`](./example_4_ai_summary.yaml)  
**设计逻辑**：RAG（检索增强生成）模式

### 填充逻辑与知识检索输入

1.  **准备上下文**:
    *   `refs`: 实时读取前一个区段「监控指标快照」的渲染结果。
    *   `queries`: 执行补充的「故障历史」SQL 查询。
2.  **构造知识检索 (Input)**:
    *   **Subject**: `{device_model}` (如：KD-PUMP-204A)
    *   **Symptoms**: 动态筛选 `realtime_metrics` 中状态为「异常」的测点作为征兆。
    *   **Objective**: 固定描述为「获取行业诊断建议」。
3.  **合成生成**: 将以上背景信息注入 `prompt` 调用 LLM。

### 渲染效果预览

```
+════════════════════════════════════════ 监控指标快照 ═════════════════════════════════════════+
│      测点        │     数值     │ 阈值 │  状态  │
│    振动幅度      │   7.8 mm/s   │ 7.5  │  异常  │
│    轴承温度      │    82.1 ℃    │ 90.0 │  正常  │
+═══════════════════════════════════ AI 专家评估与检修建议 ════════════════════════════════════+
│    评估建议    │  [AI 生成内容]：当前设备振动值(7.8)已超限。结合知识库分析，此类       │
│                │  水泵在振动超限时核心风险点在于转子不平衡。建议步骤：1. 检查基座      │
│                │  螺栓... 2. 检查轴承状态...                                         │
```

---

## 示例 5：所见即所得大纲交互（Template Copilot 改写）

**对应文件**：[`example_5_wysiwyg_outline.yaml`](./example_5_wysiwyg_outline.yaml)  
**设计逻辑**：展示大纲 `outline` 层记录的用户修改，是如何被 Agent 翻译成底层的 `nl2sql` 数据集和 `chart` 展现的。

### 场景与机制

1. **草稿期**：系统根据 `draft_prompt` 生成了最初的纯文本结论，存入 `original_draft`。
2. **定稿期 (Diff)**：用户在大纲界面补充了“发现漏油，加个折线图”，存入 `user_edited`。
3. **Agent 编译重构**：
   - Agent 识别“发现漏油”，将其作为上下文注入到原来的 `ai_synthesis` 的 Prompt 提示词中。
   - Agent 识别“加折线图”，动态创建了一个全新的 `nl2sql` 数据集用于查询近3天数据，并在 `presentation` 中插入了一块 `chart(line)` 组件。

这种将非结构化的自由文本翻译拼接为结构化 YAML/SQL 的做法，在大纲的灵活性与底层客观数据的严谨性之间找到了平衡。

---

## 示例 6：架构演化生命周期推演 (Pipeline Evolution)

**对应文件**：[`example_6_pipeline_evolution.md`](./example_6_pipeline_evolution.md)  
**核心论证**：
本文不关注具体的排版语法，重点关注**模板在系统内的 4 个状态生命周期跳转**：`State 0 (空骨架)` $\to$ `State 1 (参数就位)` $\to$ `State 2 (大纲蓝图锁定)` $\to$ `State 3 (Agent 翻译底层骨架)`。
## 示例 7：宏观架构演化真实体感渲染 (Rendered Mockups)

**对应文件**：[`example_7_rendered_mockups.md`](./example_7_rendered_mockups.md)  
**核心论证**：
这是配合示例 6 的最终阶段（State 3 编译完成并由 Data Engine 执行跑批后）的**终态视觉效果模拟**。
分别绘制了“麦当劳园区核心 IT 设施健康周报”的数据抓取预警表格，以及“金拱门多门店网络状况”基于 `foreach` 裂变产生的大量可视化图表（通过 Mermaid 展示 Line、Bar 结构与大数字卡片），让读者直接体会大纲解耦后的最终业务交付体验。
